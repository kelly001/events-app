# 2026-07-24 progress notes

## Completed today

- Installed the Phase 5 transport dependencies:
  - `@trpc/server@11.18.0`
  - `@trpc/client@11.18.0`
  - `@trpc/react-query@11.18.0`
  - `@tanstack/react-query@5.101.4`
- Added shared Zod schemas for event types, sources, cities, complete events, and the `{ events, updatedAt }` response.
- Made the browser event-cache loader validate parsed localStorage data rather than trusting an unchecked cast.
- Made localStorage read/write failures non-fatal.
- Added the tRPC context, initialization, `eventsRouter`, root router, and exported `AppRouter` type.
- Added the App Router Fetch handler at `app/api/trpc/[trpc]/route.ts`.
- Added the typed tRPC React client and stable tRPC/QueryClient providers.
- Wrapped the existing app from the server root layout without converting the layout into a Client Component.
- Replaced `fetch('/api/events')` in `useEvents` with `trpc.events.list.useQuery()`.
- Preserved the existing `useEvents` return contract, six-hour freshness calculation, cache-after-mount hydration behavior, and cached-data error fallback.
- Removed the old `app/api/events/route.ts` backend path.

## Verification

- Baseline and final `npx tsc --noEmit`: passed.
- Added the minimal `next/core-web-vitals` ESLint configuration so the existing lint script can run non-interactively.
- Final `npm run lint`: passed with no warnings or errors.
- Development server root request: HTTP 200.
- Development tRPC client request: returned seven mock events and an ISO `updatedAt` value.
- `npm run build`: passed and generated `/api/trpc/[trpc]` as the dynamic API route.
- Production server root request: HTTP 200.
- Production `events.list` request: returned seven mock events and a valid ISO timestamp.
- Production legacy `/api/events` request: HTTP 404.
- Source scan: no `/api/events` reference or direct `fetch()` remains under `app`, `components`, or `src`.
- Client/server boundary scan: `src/trpc/client.ts` imports `AppRouter` with `import type`; only the server router imports `mockEvents`.
- `git diff --check`: passed.

The successful build emitted non-fatal `EPERM` warnings while renaming two Webpack cache pack files after compilation. The output and route generation completed successfully.

The in-app browser runtime was unavailable, so the warm/stale/malformed localStorage scenarios, blocked-network warning text, hydration console, and visual regression checklist still need a manual browser pass.

## Preserved contracts

- Cache key: `ru-events-helsinki.cached-events`
- Cache/response timestamp: `updatedAt`
- Backend procedure: `events.list`
- Backend endpoint: `/api/trpc`
- Filtering remains client-side.
- Real event-source fetching remains Phase 6 work.
